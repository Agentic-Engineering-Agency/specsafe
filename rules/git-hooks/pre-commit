#!/bin/bash
# SpecSafe pre-commit hook

echo "🔍 Running SpecSafe pre-commit validation..."

# Check PROJECT_STATE.md exists
if [ ! -f "PROJECT_STATE.md" ]; then
    echo "❌ PROJECT_STATE.md not found"
    echo "   Run 'specsafe init' to set up your project"
    exit 1
fi

# Check for TODO markers in staged files only
STAGED_FILES=$(git diff --cached --name-only --diff-filter=ACM -- '*.ts' '*.js')
if [ -n "$STAGED_FILES" ] && echo "$STAGED_FILES" | xargs grep -l "TODO.*spec" 2>/dev/null | head -1 >/dev/null; then
    echo "⚠️  Found TODO items mentioning specs - review before committing"
fi

echo "✅ SpecSafe pre-commit checks passed"
