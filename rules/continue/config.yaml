# Continue.dev configuration for SpecSafe projects
# https://docs.continue.dev/

name: SpecSafe
version: 1.0.0

# Prompts for spec-driven development
prompts:
  - uses: file://prompts/specsafe.md
  - uses: file://prompts/spec.md
  - uses: file://prompts/validate.md

# Context configuration
context:
  include:
    - PROJECT_STATE.md
    - specs/active/*.md
