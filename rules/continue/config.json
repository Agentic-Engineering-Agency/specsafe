{
  "name": "SpecSafe",
  "version": "1.0.0",
  "customCommands": [
    {
      "name": "specsafe",
      "description": "Show SpecSafe project status",
      "prompt": "Read PROJECT_STATE.md and summarize: 1) Active specs and their stages, 2) What spec is currently being worked on, 3) What the next steps should be."
    },
    {
      "name": "spec",
      "description": "Show details for a specific spec by ID",
      "prompt": "Read the spec file from specs/active/{{input}}.md and show: requirements, scenarios, current stage, and implementation files."
    },
    {
      "name": "validate",
      "description": "Validate current implementation against spec",
      "prompt": "Check if the current code changes satisfy the requirements in the active spec. Point out any gaps or issues."
    }
  ],
  "context": {
    "include": ["PROJECT_STATE.md", "specs/active/*.md"]
  }
}
